# Copy these into Coolify service 'Environment Variables' BEFORE first deploy (UI, v4.3.11 API limitation)
# Choose STRONG values — these example values are placeholders only.
POSTGRES_USER=lge_admin
POSTGRES_DB=lge
POSTGRES_PASSWORD=<generate-strong-password>
MINIO_ROOT_USER=lge_minio
MINIO_ROOT_PASSWORD=<generate-strong-password>
